# 📦 دليل نشر English Kids Quest — حزمة جاهزة للتشغيل

الحزمة دي جاهزة 100% — **مفيش حاجة محتاجة build أو تنصيب برمجيات إضافية**.
اختار واحدة من طريقتين النشر حسب استضافتك:

---

## 📂 محتويات الحزمة

| المجلد | الاستخدام |
|--------|-----------|
| `site/` | نسخة ثابتة (Static) — لـ Netlify / Cloudflare / cPanel / أي استضافة ملفات |
| `node-server/` | نسخة سيرفر Node كاملة — لـ VPS أو Render أو Railway (بتشغل نفسك بأمر واحد) |

الاتنين فيهم نفس التطبيق كامل: 26 حرف + 50 جملة + 3 ألعاب + كل الصوتيات والصور **مدمجة محليًا** (مش محتاجين إنترنت لخدمات خارجية).

---

## ✅ الطريقة 1: استضافة ثابتة (الأسهل — 5 دقايق)

ارفع محتويات مجلد `site/` بالكامل على أي خدمة من دول:

### Netlify (مجاني)
1. افتح https://app.netlify.com/drop
2. اسحب مجلد `site/` كامل وارميه في الصفحة
3. هيديك رابط فوري بشهادة HTTPS ✅

### Cloudflare Pages (مجاني)
1. لوحة Cloudflare ← Pages ← Create project ← **Direct Upload**
2. ارفع مجلد `site/`
3. HTTPS تلقائي ✅

### cPanel / استضافة عادية (Public_html)
1. اضغط محتويات `site/` في ملف ZIP
2. ارفعه من File Manager لـ `public_html` واعمل Extract
3. مهم: خلي ملف `404.html` موجود جنب `index.html` (ده اللي بيخلي روابط الألعاب الداخلية تشغل)
4. لو مفيش HTTPS، **ميكروفون النطق مش هيشتغل** — فعّل شهادة SSL مجانية من Let's Encrypt من لوحة التحكم

> 💡 ملاحظة: النسخة دي مبنيّة بمسار جذر `/` — يعني تشتغل على الدومين الرئيسي مباشرة.

---

## 🖥️ الطريقة 2: سيرفر Node (للـ VPS أو منصات النشر)

### على VPS (أوبونتو مثلاً)
```bash
# المتطلبات: Node.js نسخة 18 أو أحدث (apt install nodejs)
cd node-server
sh start.sh                    # بيشتغل على البورت 3000

# أو حدد بورت معين:
PORT=80 sh start.sh            # يشتغل مباشرة على http://IP-السيرفر
```

### على Render / Railway / Fly.io
- **Start Command:** `npm start` (أو `sh start.sh`)
- **المنصة هتحدد PORT لوحدها** والسيرفر بياخده تلقائيًا ✅
- مش محتاج Build Command — الحزمة جاهزة
- لو المنصة بتطلب repo، ارفع محتوى `node-server/` على GitHub واربطه

### تشغيل دائم على VPS (اختياري)
```bash
npm install -g pm2
pm2 start index.js --name kids-quest
pm2 save && pm2 startup        # يشتغل تلقائيًا بعد إعادة التشغيل
```

---

## 🔒 ملاحظة مهمة عن الميكروفون

تمرين النطق بيستخدم ميكروفون المتصفح، والمتصفحات بتركب الميكروفون **فقط على HTTPS أو localhost**:
- Netlify / Cloudflare / Render / Railway ← بيدوك HTTPS تلقائي ✅
- VPS عادي بـ HTTP ← التطبيق كله شغال، بس تمرين النطق هيعرض رسالة إن الميكروفون غير متاح. حلّها بـ Cloudflare مجاني قدام الدومين أو شهادة Let's Encrypt.

---

## 🌐 والنسخة اللي على GitHub Pages؟

شغالة بالفعل ومتاحة على:
**https://mohannadcontento-sketch.github.io/english-kids-quest/**

وبيتحدث تلقائيًا مع كل تعديل جديد على المستودع. الحزمة دي مجرد نسخ إضافية لو حبيت ترفعها على دومينك الخاص.

---

## 🧪 اختبرت إيه قبل تسليم الحزمة؟

- ✅ السيرفر اشتغل مستقل على بورت تجريبي وكل المسارات رجعت 200:
  `/` و `/letters` و `/sentences` و `/games` + عينات الصوت والصور
- ✅ 26 ملف صوت للحروف + 26 كلمة + 50 جملة كلها موجودة
- ✅ ملف `404.html` مضاف للنسخة الثابتة عشان التنقل الداخلي يشتغل على أي استضافة
